<!DOCTYPE html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Dashboard</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.8.0/css/bulma.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.8.0/css/bulma.css.map">
    <style>
        .form__container {
            display: block;
            width: 100%;
        }

        .form {
            margin: 50px auto;
            width: 350px;
        }

        table {
            margin: 50px auto;
        }
        .log-out-form {
            font-size: 14px;
            display: block;
            margin: 0 auto;
            width: 350px;
            border: 1px solid black;
            padding: 10px;
        }
        .log-out-btn {
            margin: 0 auto;
            display: block;
        }
    </style>
</head>

<body>
    <section>
        <main>
            <h1 class="title card-footer-item">welcome to your e-class</h1>
            <h3 class="title card-footer-item">Lessons</h3>
        </main>
        <main class="form__container container">
            <table class="table">
                <thead>
                    <tr>
                        <th><abbr title="No">No</abbr></th>
                        <th><abbr title="Name">Name</abbr></th>
                        <th><abbr title="Semester">Semester</abbr></th>
                        <th><abbr title="Grade">Grade</abbr></th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                    </tr>
                </tfoot>
                <tbody>
                    <% for(var i = 0; i < lessons.length; i++) { %>
                    <tr>
                        <th><%= i+1 %></th>
                        <td><%= lessons[i].name %></td>
                        <td><%= lessons[i].semester %></td>
                        <td><%= lessons[i].grade %></td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
            <form method="POST" action="/api/signOut" class="log-out-form">
                <p class="title card-footer-item">Sign out</p>
                <input type="submit" class="log-out-btn button is-warning" value="Log out"/>
            </form>
        </main>
    </section>
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</body>

</html>